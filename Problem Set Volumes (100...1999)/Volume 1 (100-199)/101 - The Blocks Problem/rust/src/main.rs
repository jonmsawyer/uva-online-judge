//use rust::Blocks;

fn main() {
    //let mut blocks = Blocks(vec![vec![0], vec![1], vec![2]]);
    //blocks.move(1).onto(3);
    //blocks.pile(3).over(2);
    //println!("blocks = {:?}", blocks);
    println!("Hello, world!");
}
